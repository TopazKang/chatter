# ============================================
# 회사 주차 관리 서비스 - 환경 변수 설정
# ============================================
# 이 파일을 복사하여 .env 파일을 생성하세요:
# cp .env.example .env
#
# 보안 주의사항:
# - .env 파일은 절대 Git에 커밋하지 마세요
# - 프로덕션 환경에서는 강력한 패스워드를 사용하세요
# - 민감한 정보는 시크릿 관리 도구 사용을 권장합니다

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
# PostgreSQL 사용자 이름
POSTGRES_USER=parkingadmin

# PostgreSQL 패스워드 (프로덕션에서는 반드시 변경!)
# 최소 16자 이상, 대소문자/숫자/특수문자 조합 권장
POSTGRES_PASSWORD=securepassword

# 데이터베이스 이름
POSTGRES_DB=parking_management

# 데이터베이스 호스트 (Docker Compose 내부 서비스명)
DATABASE_HOST=database

# 데이터베이스 포트 (외부 노출 포트)
POSTGRES_PORT=5432

# PostgreSQL 인증 방법
# - scram-sha-256: 가장 안전한 방법 (권장)
# - md5: 하위 호환성이 필요한 경우
POSTGRES_HOST_AUTH_METHOD=scram-sha-256

# PostgreSQL 데이터 저장 경로 (호스트 머신)
POSTGRES_DATA_PATH=./data/postgres

# ============================================
# Backend Configuration (Node.js + Express)
# ============================================
# 실행 환경 (development, production, test)
NODE_ENV=production

# 백엔드 포트
BACKEND_PORT=3000

# API 경로 접두사
API_PREFIX=/api

# 로그 레벨 (error, warn, info, debug)
LOG_LEVEL=info

# CORS 허용 오리진
# - *: 모든 도메인 허용 (개발 환경)
# - https://yourdomain.com: 특정 도메인만 허용 (프로덕션)
CORS_ORIGIN=*

# JWT Secret Key (프로덕션에서는 반드시 변경!)
# 32자 이상 랜덤 문자열 권장
# JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 세션 시크릿 (향후 인증 기능 추가 시 사용)
# SESSION_SECRET=your-super-secret-session-key-change-this

# ============================================
# Frontend Configuration (React + Vite)
# ============================================
# API 엔드포인트 URL
# - 개발: http://localhost:3000/api
# - 프로덕션: https://yourdomain.com/api 또는 /api (상대 경로)
VITE_API_URL=http://localhost:3000/api

# 프론트엔드 포트
FRONTEND_PORT=80

# ============================================
# Docker Compose 설정
# ============================================
# Compose 프로젝트 이름
COMPOSE_PROJECT_NAME=parking-management

# ============================================
# 모니터링 설정 (Optional - Prometheus + Grafana)
# ============================================
# Prometheus 포트
# PROMETHEUS_PORT=9090

# Grafana 포트
# GRAFANA_PORT=3001

# Grafana 관리자 패스워드
# GRAFANA_ADMIN_PASSWORD=admin

# ============================================
# 백업 설정
# ============================================
# 백업 보관 기간 (일)
BACKUP_RETENTION_DAYS=30

# 백업 저장 경로
BACKUP_PATH=./backups

# ============================================
# 성능 튜닝
# ============================================
# PostgreSQL 최대 연결 수
POSTGRES_MAX_CONNECTIONS=100

# PostgreSQL Shared Buffers (메모리 할당)
POSTGRES_SHARED_BUFFERS=256MB

# Node.js 힙 메모리 제한 (MB)
NODE_OPTIONS=--max-old-space-size=512

# ============================================
# 개발 환경 전용 설정
# ============================================
# 핫 리로드 활성화 (frontend)
# VITE_HMR=true

# 디버그 모드
# DEBUG=app:*
